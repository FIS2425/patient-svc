apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-patient
  name: mongo-patient
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
